# Use the official Arch Linux base image
FROM archlinux:latest

# 1) Sync the package databases and upgrade any installed packages
# 2) Install the minimal base-devel group, git, curl, and sudo so the container is usable
# 3) Clean up pacman’s cache to minimize image size
RUN pacman -Syu --noconfirm \
    && pacman -S --noconfirm \
        base-devel \
        git \
        curl \
        sudo \
    && pacman -Scc --noconfirm

# (Optional) Create a non-root user (codespace) with sudo privileges
# so that you’re not always running as root inside Codespace.
RUN useradd -m codespace \
    && echo "codespace ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers

# Switch to that user by default
USER codespace
WORKDIR /home/codespace

# Export UTF-8 locale (Arch’s default Docker image is quite minimal).
ENV LANG=en_US.UTF-8
